import React from 'react';

const Transactions = () => {
  return (
    <div>
      Transactions: should implement a table to fetch all transactions sent to arweave from the connected user address
      with graphql and display it tabularly
    </div>
  );
};

export default Transactions;
